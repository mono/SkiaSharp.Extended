trigger:
  branches:
    include:
      - main
  paths:
    exclude:
      - README.md
      - CODE-OF-CONDUCT.md
      - LICENSE
      - .github/
      - docs/

pr:
  branches:
    include:
      - main
  paths:
    exclude:
      - README.md
      - CODE-OF-CONDUCT.md
      - LICENSE
      - .github/
      - docs/
      - images/

variables:
  - template: /scripts/azure-pipelines-variables.yml@self

pool:
  vmImage: windows-latest

jobs:
  - job: build
    displayName: Build
    steps:
      - template: /scripts/azure-pipelines-steps-prepare.yml@self
      - pwsh: dotnet cake --target=build
        displayName: Build everything
      - publish: output
        artifact: build
        condition: always()
        displayName: Upload Logs

  - job: pack
    displayName: Pack
    steps:
      - template: /scripts/azure-pipelines-steps-prepare.yml@self
      - pwsh: dotnet cake --target=pack
        displayName: Pack NuGet packages
      - publish: output
        artifact: pack
        condition: always()
        displayName: Upload Logs

  - job: test
    displayName: Test
    steps:
      - template: /scripts/azure-pipelines-steps-prepare.yml@self
      - pwsh: dotnet cake --target=test
        displayName: Run tests
      - task: PublishTestResults@2
        condition: always()
        inputs:
          testResultsFormat: VSTest
          testResultsFiles: output/test-results/*.trx
      - publish: output
        artifact: test
        condition: always()
        displayName: Upload Logs

  - job: api_compat
    displayName: API Compatibility Check
    dependsOn: test
    pool:
      vmImage: windows-latest
    steps:
      - checkout: self
      - pwsh: dotnet tool restore
        displayName: Restore .NET tools
      - pwsh: dotnet build source/SkiaSharp.Drawing/SkiaSharp.Drawing.csproj -c Release
        displayName: Build SkiaSharp.Drawing
      - pwsh: >-
          dotnet apicompat
          --left tools/api-baseline/netstandard2.0/System.Drawing.Common.dll
          --right source/SkiaSharp.Drawing/bin/Release/netstandard2.0/System.Drawing.Common.dll
          --strict-mode
          --suppression-file tools/api-baseline/api-compat-suppressions.xml
        displayName: Run API compatibility check

  - job: generate_references
    displayName: Generate Reference Images
    pool:
      vmImage: windows-latest
    steps:
      - template: /scripts/azure-pipelines-steps-prepare.yml@self
      - pwsh: |
          $env:SCENARIO_OUTPUT_PATH = "$(Build.ArtifactStagingDirectory)/reference-images"
          dotnet test tools/SkiaSharp.Drawing.ReferenceGenerator --nologo
        displayName: Generate reference images with real System.Drawing
      - publish: $(Build.ArtifactStagingDirectory)/reference-images
        artifact: reference-images
        displayName: Upload reference images

  - job: pixel_compat
    displayName: Pixel Compatibility Tests
    dependsOn: generate_references
    pool:
      vmImage: windows-latest
    steps:
      - template: /scripts/azure-pipelines-steps-prepare.yml@self
      - download: current
        artifact: reference-images
      - pwsh: |
          $env:REFERENCE_IMAGES_PATH = "$(Pipeline.Workspace)/reference-images"
          $env:TEST_ARTIFACTS_PATH = "$(Build.ArtifactStagingDirectory)/diff-images"
          dotnet test tests/SkiaSharp.Drawing.Tests --filter "FullyQualifiedName~PixelCompat" --logger trx --results-directory "$(Build.ArtifactStagingDirectory)/test-results"
        displayName: Run pixel compatibility tests
      - task: PublishTestResults@2
        condition: always()
        inputs:
          testResultsFormat: VSTest
          testResultsFiles: $(Build.ArtifactStagingDirectory)/test-results/*.trx
          testRunTitle: Pixel Compatibility
      - publish: $(Build.ArtifactStagingDirectory)/diff-images
        artifact: diff-images
        condition: always()
        displayName: Upload diff images (actual + reference + diff mask)
      - publish: $(Build.ArtifactStagingDirectory)/test-results
        artifact: pixel-compat-results
        condition: always()
        displayName: Upload test results
