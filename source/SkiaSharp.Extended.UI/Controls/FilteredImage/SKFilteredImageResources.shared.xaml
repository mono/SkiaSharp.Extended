<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:local="clr-namespace:SkiaSharp.Extended.UI.Controls"
                    xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
                    x:Class="SkiaSharp.Extended.UI.Controls.Themes.SKFilteredImageResources">

    <!-- the control template for SKFilteredImage -->
    <ControlTemplate x:Key="SKFilteredImageControlTemplate">
        <skia:SKGLView x:Name="PART_DrawingSurface" />
    </ControlTemplate>

    <!-- the explicit style that allows for extension -->
    <Style x:Key="SKFilteredImageStyle" TargetType="local:SKFilteredImage">
        <Setter Property="ControlTemplate"
                Value="{StaticResource SKFilteredImageControlTemplate}" />
    </Style>

    <!-- the implicit style that applies to all controls -->
    <Style TargetType="local:SKFilteredImage"
           BasedOn="{StaticResource SKFilteredImageStyle}" />

</ResourceDictionary>